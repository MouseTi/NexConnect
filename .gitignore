# Staging & transient
stage/

# Logs / debug
*.log
*.err

# Backups
*.bak

# Binary artifacts (uploaded as GitHub Release assets, not committed)
*.exe
*.dll
*.zip
*.7z
*.msi
*.nsis

# Per-user local manifests (never commit the override)
manifest.local.json

# Local build log (do not commit — transient)
BUILD_NOTE.md